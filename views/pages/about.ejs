<!DOCTYPE html>
<html lang="en">
<head>
  <title>About Us</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body style="background: url('/images/background-image.jpg') no-repeat center center fixed; background-size: cover;">
<%- include('../partials/header', { title: 'About Us' }) %>

<h1 class="text-center mb-5 text-white">Meet the Team</h1>

<div class="d-flex flex-column gap-4">
  <% team.forEach(member => { %>
    <div class="card bg-transparent border border-light text-white p-4" style="min-height: 250px;">
      <div class="card-body">
        <h3 class="card-title"><%= member.name %></h3>
        <h5 class="card-subtitle mb-2 text-light"><%= member.role %></h5>
        <p class="card-text mt-3"><%= member.description %></p>
      </div>
    </div>
  <% }); %>
</div>
<%- include('../partials/footer') %>
</body>
</html>

